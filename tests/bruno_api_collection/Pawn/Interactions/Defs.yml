info:
  name: Defs
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/v1/game/defs/interactions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
