info:
  name: Pawn Interactions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/v1/pawns/interactions?pawn_id=779"
  params:
    - name: pawn_id
      value: "779"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
