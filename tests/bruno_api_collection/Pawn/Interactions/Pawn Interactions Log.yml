info:
  name: Pawn Interactions Log
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/v1/pawns/interactions/log?pawn_id=779&limit=50"
  params:
    - name: pawn_id
      value: "779"
      type: query
    - name: limit
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
