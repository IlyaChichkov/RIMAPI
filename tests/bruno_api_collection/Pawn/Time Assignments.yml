info:
  name: Time Assignments
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseURL}}/api/v1/time-assignments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
