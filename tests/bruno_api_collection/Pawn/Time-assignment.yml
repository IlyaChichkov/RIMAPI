info:
  name: Time-assignment
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseURL}}/api/v1/colonist/time-assignment"
  auth: inherit

body:
  json: |-
    {
            "pawn_id": 99,
            "hour": 14,
            "assignment": "Joy"
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
