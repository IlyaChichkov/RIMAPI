info:
  name: Work-list
  type: http
  seq: 17

http:
  method: GET
  url: "{{baseURL}}/api/v1/work-list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
