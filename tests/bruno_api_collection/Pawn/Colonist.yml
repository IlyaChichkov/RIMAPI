info:
  name: Colonist
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/v1/colonist?id=1020"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
