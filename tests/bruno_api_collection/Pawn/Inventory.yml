info:
  name: Inventory
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseURL}}/api/v1/colonist/inventory?id=1020"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
