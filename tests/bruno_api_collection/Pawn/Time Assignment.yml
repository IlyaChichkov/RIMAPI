info:
  name: Time Assignment
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseURL}}/api/v1/colonist/time-assignment"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
