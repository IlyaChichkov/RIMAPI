info:
  name: Trait Def
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseURL}}/api/v1/trait-def?name=Ascetic"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
