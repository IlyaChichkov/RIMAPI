info:
  name: Outfits
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseURL}}/api/v1/outfits"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
