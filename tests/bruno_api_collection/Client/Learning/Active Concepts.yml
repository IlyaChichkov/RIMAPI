info:
  name: Active Concepts
  type: http
  seq: 26

http:
  method: GET
  url: "{{baseURL}}/api/v1/client/learning/active"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
