info:
  name: Concepts Defs
  type: http
  seq: 28

http:
  method: GET
  url: "{{baseURL}}/api/v1/client/learning/defs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
