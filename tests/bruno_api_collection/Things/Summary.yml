info:
  name: Summary
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseURL}}/api/v1/resources/storages/summary?map_id=0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
