info:
  name: Recipes
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/v1/item/recipes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
