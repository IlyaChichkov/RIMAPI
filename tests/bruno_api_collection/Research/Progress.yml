info:
  name: Progress
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/v1/research/progress"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
