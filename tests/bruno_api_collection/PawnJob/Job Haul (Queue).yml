info:
  name: Job Haul (Queue)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/api/v1/pawn/job"
  body:
    type: json
    data: |-
      {
        "pawn_id": 215,
        "job_def": "Haul",
        "target_thing_id": 36795,
        "queue": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
