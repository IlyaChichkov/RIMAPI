info:
  name: Needs
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/api/v1/pawn/edit/needs"
  auth: inherit

body:
  json: |-
    {
            "pawn_id": 148,
            "food": 1.0,
            "rest": 1.0,
            "mood": 1.0
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
