info:
  name: Faction
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseURL}}/api/v1/pawn/edit/faction"
  auth: inherit

body:
  json: |-
    {
            "pawn_id": 148,
            "make_colonist": true
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
