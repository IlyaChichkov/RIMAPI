info:
  name: Health
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/api/v1/pawn/edit/health"
  auth: inherit

body:
  json: |-
    {
            "pawn_id": 148,
            "heal_all_injuries": true,
            "restore_body_parts": true,
            "remove_all_diseases": true
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
