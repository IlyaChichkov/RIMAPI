info:
  name: Position
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseURL}}/api/v1/pawn/edit/position"
  auth: inherit

body:
  json: |-
    {
            "pawn_id": 148,
            "position": { "x": 100, "z": 100 }
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
