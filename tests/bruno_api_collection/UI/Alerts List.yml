info:
  name: Alerts List
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/v1/ui/alerts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5