info:
  name: Spawn
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/api/v1/pawn/spawn"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
