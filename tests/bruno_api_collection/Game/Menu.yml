info:
  name: Menu
  type: http
  seq: 18

http:
  method: POST
  url: "{{baseURL}}/api/v1/game/main-menu"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
