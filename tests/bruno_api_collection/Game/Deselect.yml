info:
  name: Deselect
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseURL}}/api/v1/deselect"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
