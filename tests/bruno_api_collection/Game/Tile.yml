info:
  name: Tile
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseURL}}/api/v1/datetime"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
