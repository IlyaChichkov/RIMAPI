info:
  name: Mods List
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/v1/mods/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
