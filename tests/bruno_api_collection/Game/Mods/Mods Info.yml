info:
  name: Mods Info
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}/api/v1/mods/info?uid=brrainz.harmony"
  params:
    - name: uid
      value: brrainz.harmony
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
