info:
  name: Mod Preview
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/v1/mods/preview?uid=brrainz.harmony"
  params:
    - name: uid
      value: brrainz.harmony
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
