info:
  name: Quit
  type: http
  seq: 20

http:
  method: POST
  url: "{{baseURL}}/api/v1/game/quit"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
