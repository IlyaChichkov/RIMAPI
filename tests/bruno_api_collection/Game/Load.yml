info:
  name: Load
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseURL}}/api/v1/game/load"
  body:
    type: json
    data: |-
      {
        "file_name": "AutoTestingScenario",
        "check_version": false,
        "skip_mod_mismatch": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
