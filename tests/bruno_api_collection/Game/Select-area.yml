info:
  name: Select-area
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseURL}}/api/v1/game/select-area"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
