info:
  name: Letter
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseURL}}/api/v1/game/send/letter"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
