info:
  name: Open-tab
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseURL}}/api/v1/open-tab?name=health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
