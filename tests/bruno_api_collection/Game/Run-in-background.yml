info:
  name: Run-in-background
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/api/v1/game/settings/toggle/run-in-background"
  auth: inherit

body:
  json: |-
    true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
