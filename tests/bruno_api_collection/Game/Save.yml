info:
  name: Save
  type: http
  seq: 16

http:
  method: POST
  url: "{{baseURL}}/api/v1/game/save"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
