info:
  name: Speed
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/api/v1/game/speed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
