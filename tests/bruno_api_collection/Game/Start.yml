info:
  name: Start
  type: http
  seq: 17

http:
  method: POST
  url: "{{baseURL}}/api/v1/game/start"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
