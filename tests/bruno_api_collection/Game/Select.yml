info:
  name: Select
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseURL}}/api/v1/select?type=pawn&id=443"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
