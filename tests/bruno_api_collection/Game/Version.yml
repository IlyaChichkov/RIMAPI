info:
  name: Version
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseURL}}/api/v1/version"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
