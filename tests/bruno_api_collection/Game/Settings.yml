info:
  name: Settings
  type: http
  seq: 20

http:
  method: GET
  url: "{{baseURL}}/api/v1/game/settings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
