info:
  name: Datetime
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseURL}}/api/v1/datetime"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
