info:
  name: Incidents
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/api/v1/incidents?map_id=0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
