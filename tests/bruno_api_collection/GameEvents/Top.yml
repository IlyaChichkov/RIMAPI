info:
  name: Top
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/api/v1/incidents/top"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
