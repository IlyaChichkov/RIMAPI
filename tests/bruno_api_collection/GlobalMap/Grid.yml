info:
  name: Grid
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseURL}}/api/v1/world/grid"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
