info:
  name: Sites
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/v1/world/sites"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
