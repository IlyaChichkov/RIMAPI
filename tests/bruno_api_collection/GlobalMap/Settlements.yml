info:
  name: Settlements
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}/api/v1/world/player/settlements"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
