info:
  name: Area
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseURL}}/api/v1/world/grid/area?tile_id=12345&radius=5"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
