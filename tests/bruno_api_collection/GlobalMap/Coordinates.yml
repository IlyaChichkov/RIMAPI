info:
  name: Coordinates
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseURL}}/api/v1/world/tile/coordinates?id=12345"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
