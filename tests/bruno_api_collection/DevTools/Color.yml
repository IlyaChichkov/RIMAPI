info:
  name: Color
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}/api/v1/dev/stuff/color"
  auth: inherit

body:
  json: |-
    {
            "name": "WoodLog",
            "hex": "aa00ff"
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
