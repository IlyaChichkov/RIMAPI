info:
  name: Endpoint List
  type: http
  seq: 26

http:
  method: GET
  url: "{{baseURL}}/api/v1/dev/endpoints"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
