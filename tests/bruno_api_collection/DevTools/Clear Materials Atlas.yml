info:
  name: Clear Materials Atlas
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/api/v1/dev/materials-atlas/clear"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
