info:
  name: Materials Atlas
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/v1/dev/materials-atlas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
