info:
  name: Faction
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/api/v1/faction?id=2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
