info:
  name: Icon
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseURL}}/api/v1/faction/icon"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
