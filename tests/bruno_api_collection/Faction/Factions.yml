info:
  name: Factions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/v1/factions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
