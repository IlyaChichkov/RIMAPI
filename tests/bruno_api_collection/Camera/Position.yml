info:
  name: Position
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/api/v1/camera/change/position?x=50&y=50"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
