info:
  name: Zoom
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/api/v1/camera/change/zoom?zoom=15"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
