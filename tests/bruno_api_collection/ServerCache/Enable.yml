info:
  name: Enable
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/api/v1/cache/enable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
