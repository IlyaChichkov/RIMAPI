info:
  name: Disable
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/api/v1/cache/disable"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
