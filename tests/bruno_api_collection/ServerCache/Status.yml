info:
  name: Status
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/v1/cache/status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
