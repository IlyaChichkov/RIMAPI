info:
  name: Things
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/v1/map/things?map_id=0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
