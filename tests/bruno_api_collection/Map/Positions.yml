info:
  name: Positions
  type: http
  seq: 24

http:
  method: POST
  url: "{{baseURL}}/api/v1/map/repair/positions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
