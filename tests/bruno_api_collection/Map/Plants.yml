info:
  name: Plants
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseURL}}/api/v1/map/plants"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
