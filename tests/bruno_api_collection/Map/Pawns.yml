info:
  name: Pawns
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/v1/map/pawns?mapId=0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
