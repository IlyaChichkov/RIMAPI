info:
  name: Radius
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseURL}}/api/v1/map/things/radius"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
