info:
  name: Terrain
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/api/v1/map/terrain?map_id=0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
