info:
  name: Buildings
  type: http
  seq: 20

http:
  method: GET
  url: "{{baseURL}}/api/v1/map/buildings?map_id=0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
