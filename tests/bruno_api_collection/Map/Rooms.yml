info:
  name: Rooms
  type: http
  seq: 19

http:
  method: GET
  url: "{{baseURL}}/api/v1/map/rooms?map_id=0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
