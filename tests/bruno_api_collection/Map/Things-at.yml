info:
  name: Things-at
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseURL}}/api/v1/map/things-at"
  auth: inherit

body:
  json: |-
    {
            "map_id": 0,
            "position": { "x": 15, "y": 0, "z": 25 }
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
