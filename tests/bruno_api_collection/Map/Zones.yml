info:
  name: Zones
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseURL}}/api/v1/map/zones?map_id=0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
