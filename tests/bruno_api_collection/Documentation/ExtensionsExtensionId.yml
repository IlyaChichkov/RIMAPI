info:
  name: ExtensionsExtensionId
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/v1/docs/extensions/34"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
