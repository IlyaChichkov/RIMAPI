info:
  name: Available Recipes
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseURL}}/api/v1/buildings/recipes?building_id=25345"
  params:
    - name: building_id
      value: "25345"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
