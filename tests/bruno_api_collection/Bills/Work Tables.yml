info:
  name: Work Tables
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseURL}}/api/v1/map/work-tables?map_id=0"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
