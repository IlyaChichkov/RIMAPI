info:
  name: Reorder Bill
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseURL}}/api/v1/buildings/bill/reorder?building_id=25345&bill_id=0"
  params:
    - name: building_id
      value: "25345"
      type: query
    - name: bill_id
      value: "0"
      type: query
  body:
    type: json
    data: |-
      {
        "offset": -1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: If there are fewer recipes in the queue, the recipe will disappear.
