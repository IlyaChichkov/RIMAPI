info:
  name: Get Bill
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/v1/buildings/bill?building_id=25345&bill_id=3"
  params:
    - name: building_id
      value: "25345"
      type: query
    - name: bill_id
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
