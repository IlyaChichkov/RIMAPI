info:
  name: Announce
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/api/v1/ui/announce"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
